using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bread : Structures
{
    // Start is called before the first frame update
    void Start()
    {
        Begin(100, 0, 10, 1);
        DP = 100;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
